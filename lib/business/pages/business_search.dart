import 'package:flutter/material.dart';

// this will be a search page with optional AI features. It should have a list of 
// options for narrowing down search and then a search bar at the bottom for AI powered search.

class BusinessSearch extends StatefulWidget {
  const BusinessSearch({super.key});

  @override
  State<BusinessSearch> createState() => _BusinessSearchState();
}

class _BusinessSearchState extends State<BusinessSearch> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
